cat > ~/render.yaml << 'EOF'
services:
  - type: web
    name: mrctv2
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT playeraonvivotteste:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
EOF
